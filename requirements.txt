# Python 3.10+ recommended. Install with: pip install -r requirements.txt

# HTTP downloads and API access (01, 02, 05)
requests>=2.31
urllib3>=2.0

# Tabular data processing (03-05)
pandas>=2.0

# Spatial join with ABS SA4 boundaries (04)
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyogrio>=0.7

# Database (06)
duckdb>=1.5,<1.6

# Exploratory notebook (notebooks/)
jupyter
